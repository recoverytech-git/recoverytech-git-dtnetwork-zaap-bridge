export const metadata = {
  title: "DTNetwork → Zaap Bridge",
  description: "Webhook bridge independente entre DTNetwork e Zaap.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="pt-BR">
      <body>{children}</body>
    </html>
  );
}
